{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "uvicorn sellout.web.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/saude",
    "restartPolicyType": "ON_FAILURE"
  }
}
